<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN"
        "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">
<html xmlns="http://www.w3.org/1999/xhtml" xml:lang="pl" lang="pl">
  <head>
    <meta http-equiv="content-type" content="text/html;charset=UTF-8" />
    <title>Skauting Jurajski</title>
    <%= stylesheet_link_tag "reset", "text", "960", "scaffold", "facebox", "wysihat", :cache => true, :recursive => true %>
    <%= javascript_include_tag :defaults, 'wysihat_engine_pack', :cache => true, :recursive => true %>
    <%= csrf_meta_tag %>
  </head>
  <body>

    <!-- LOGO -->
    <div id="logo">
      <div class="container_16">
        <div class="grid_4">
          <%= image_tag 'logo.png', :id => 'pic_1' %>
          <%= image_tag 'logo3.png', :id => 'pic_2', :style => 'display:none' %>
        </div>
        <div class="grid_12">
          Skauting Jurajski
        </div>
      </div>
      <div class="clear"></div>
    </div>

    <!-- FLASH MESSAGE -->
    <div id="flash_message">
      <% if flash[:notice] %>
        <p id="flash_notice"><%= flash[:notice] %></p>
        <% flash[:notice] = nil %>
      <% end %>
      <% if flash[:error] %>
        <p id="flash_error"><%= flash[:error] %></p>
        <% flash[:error] = nil %>
      <% end %>
    </div>
    <div class="clear"></div>

    <!-- CONTENT -->
    <div id="main" class="container_16">
      <div class="grid_4">
        <%= render :partial => 'layouts/menu' %>
      </div>
      <div class="grid_12">
        <%= yield %>
      </div>
    </div>
    <div class="clear"></div>

    <!-- FOOTER -->
    <div id="footer">
      <div>
        <span>Copyright MacGajek 2011</span>
      </div>
    </div>

    <% javascript_tag do %>
      var counter = 1;
      var count_limit = 2;
      function changeBanner() {
        $('pic_' + counter).hide();
        counter++; if(counter > count_limit) counter = 1;
        $('pic_' + counter).show();
      }
      var timer = window.setInterval("changeBanner()", 10000);
    <% end %>

  </body>
</html>
